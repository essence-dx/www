# DX-WWW Status

> Historical workspace status, not the current 30-agent release-readiness score.
> The current 30-agent worker checkpoint is 84/100, blocked by 62 Rust warnings,
> chaotic worktree state, source-guard-heavy evidence, missing browser screenshot
> and overlay recovery proof, generated artifact curation, and no post-curation
> full `cargo test` / `cargo clippy` run.

Last updated: 2026-05-20

## Current Status

The 100/100 labels in this historical status section are source/contract slice
scores, not current live browser, runtime, or full-release proof.

Phase 0 workspace honesty is complete enough for local commit loops. The active Cargo workspace is now the flattened `G:\Dx\www` root, with generated output, local artifacts, external inspiration clones, and duplicated icon data excluded from root history. The Forge package-expansion set is complete at 100/100, the Forge launch-readiness set is complete at 100/100, the Forge launch-evidence set is complete at 100/100, the Forge package-breadth set is complete at 100/100, the Forge launch-productization set is complete at 100/100, the Forge release-hardening set is complete at 100/100, the Forge public-launch polish set is complete at 100/100, the Forge launch-budget set is complete at 100/100, the Forge CI/release operations set is complete at 100/100, the CI evidence-contract set is complete at 100/100, the Forge public-evidence portal set is complete at 100/100, the Forge public release-dashboard set is complete at 100/100, the Forge public release-history set is complete at 100/100, the Forge release-bundle/publishing set is complete at 100/100, the Forge launch-changelog distribution set is complete at 100/100, the Forge production launch hardening set is complete at 100/100, the Forge real-project adoption and credible benchmark breadth set is complete at 100/100, the Forge real-app integration and live benchmark proof set is complete at 100/100, the Forge adoption release channel and benchmark hardening set is complete at 100/100, the Forge public beta developer onboarding and release discipline set is complete at 100/100, the Forge advisory provenance and package trust expansion set is complete at 100/100, the Forge signed publishing and hosted beta operations set is complete at 100/100, and the DX-WWW Route, Page, And State Compilation Standard slice is complete at 100/100. The active TSX App Router strict parity set is now 92/100, with metadata/image/font/script/redirect/header adapters remaining.

## Verified

- Compiled React-shaped App Router routes now expose a first-class Route Unit contract with shell, graph, state, packet, receipt, and adaptive runtime report sections.
- The Route Unit state graph keeps DX-native `state()`, `derived()`, `effect()`, and `action()` as the source-owned state/action contract; React hook-shaped syntax is adapter-only inventory unless exact compiler proof lowers it into DX state slots.
- `next_app_router_conformance_report()` now compiles strict no-`node_modules` App Router fixtures for static pages, client islands, form/server actions, dynamic routes, and metadata/image/font-like surfaces.
- `dx check --project-contract` now adds LLM-friendly source standards for large source files, non-package barrel files, dynamic imports, client/server boundary mistakes, and Forge provenance gaps.
- `Cargo.toml` resolves as a Cargo workspace from `G:\Dx\www`.
- `cargo metadata --no-deps --format-version 1` succeeds from `G:\Dx\www`.
- The Forge Security MVP exists under `core/src/ecosystem`.
- `dx forge add shadcn/ui/button` materializes editable source-owned files.
- `dx add ui/button` now materializes the same editable source-owned shadcn button package by default, with `--dry-run` available for review-only installs and no `node_modules` creation.
- `dx add icon search` and `dx forge add icon/search` now materialize only the selected `SearchIcon`, a tiny local icon helper, Forge manifest/receipt state, and no `node_modules`.
- `dx add auth/better-auth` and `dx forge add auth/better-auth --write` now materialize source-owned Google OAuth starter files for config, start route, callback handler, `.env.example`, and README without creating `node_modules`.
- `dx forge init-app --write` now creates the clean public beta adoption app, materializes the current Forge launch packages, writes package docs/receipts, records a scorecard plus strict `dx check` artifact, and verifies no `node_modules` is created.
- `dx prove vertical --fixture forge-quickstart --write` now generates a static `/forge/quickstart` public beta route with claims manifest coverage for the `dx forge init-app`, strict check, CI, source-owned package review, no-`node_modules`, and honest-scope flow. `docs/forge-public-beta-quickstart.md` documents the same beta path.
- `dx forge package-gallery --project <path>` now writes terminal/JSON/Markdown reports for the curated launch packages, joining file maps, editable ownership boundaries, advisory placeholders, docs status, update preview, rollback state, scorecard evidence, and no-`node_modules` proof.
- `dx forge provenance --project <path>` now writes terminal/JSON/Markdown reports that join the source manifest, curated registry integrity, matching provenance metadata, BLAKE3 receipt hashes, accepted update receipts, declared license metadata, rollback coverage, and no-`node_modules` proof into one source-owned package provenance artifact.
- `.dx/forge/advisories.json` is now ingested by project scorecards as offline advisory metadata, using the `offline-snapshot` coverage kind so `dx forge scorecard --project <path>` and `dx forge package-gallery --project <path>` can distinguish real offline advisory coverage from curated placeholder review without network access.
- Forge release-bundle manifests now separate local BLAKE3 artifact integrity from publisher identity, record explicit `unsigned` status for current bundles, and verify `signed` manifests only when Ed25519 publisher key, key id, timestamp, and signature metadata match the release manifest digest.
- `dx forge registry smoke --remote r2 --local <path>` now writes terminal/JSON/Markdown evidence that joins local registry init, registry integrity, R2 publish dry-run planning, R2 pull dry-run planning, no-secret requirements, and no-`node_modules` proof.
- `dx forge beta-install --release-bundle <dir> --project <path> --write` now bootstraps a clean beta adoption project from an adoption-inclusive release bundle, verifies the release manifest gate, writes provenance and trust-regression artifacts, copies public Forge routes including `/forge/adoption`, and leaves a reviewable `.dx/forge/beta-install` artifact trail without creating `node_modules`.
- `dx forge package-gallery --project <path> --public-index <dir>` now writes a hosted `/forge/package-gallery/` static HTML index plus JSON and Markdown artifacts so adopters can inspect curated packages, trust signals, advisory coverage, ownership boundaries, and migration commands without running the CLI first.
- `dx forge release-operations --release-manifest <json> --trust-regression <json> --release-candidate <json> --ci-artifacts <dir> --public-evidence <dir>` now joins a signed publisher manifest, trust-regression evidence, release-candidate gate, CI artifacts, public evidence export, and no-`node_modules` boundary checks into one ready-to-ship operations gate.
- `dx forge publisher-key generate` and `dx forge publisher-key sign` now create operator-owned Ed25519 publisher key files and sign release manifests without exposing private key material in CLI reports, then the existing release-bundle verifier checks the attached publisher identity.
- `dx forge release-bundle` now includes hosted `/forge/package-gallery/` HTML, JSON, and Markdown artifacts in the BLAKE3 release manifest, while `dx forge release-operations` requires the signed manifest and package-gallery promotion check before reporting ready-to-ship.
- `dx forge publish-plan --release-bundle <dir> --pages <dir> --registry-smoke <json> --release-operations <json>` now maps Pages, R2, and local release artifacts with cache policy, rollback inputs, no-secret requirements, and no-`node_modules` checks before public beta deployment.
- `dx add ui/card` and `dx forge add shadcn/ui/card` now materialize a second curated shadcn source-owned component, and `dx forge migration-guide --package shadcn/ui/card` maps the card command to Forge receipts, docs, update preview, scorecard, and no-`node_modules` checks.
- `dx forge trust-regression --project <path>` now creates isolated package-trust fixtures that mutate local source, provenance metadata, license metadata, receipt data, and offline advisory metadata, proving green/yellow/red trust decisions remain distinguishable without creating `node_modules`.
- `dx forge release-candidate --project <path>` now combines existing CI artifacts, Pages bundle verification, public route comparison, source-owned package review, static competitor evidence, recursive secret-marker scans, and no-`node_modules` boundary checks into one terminal/JSON/Markdown pass/fail artifact.
- `dx forge migration-guide --project <path>` now writes a focused shadcn/ui button migration guide/report, mapping `npx shadcn@latest add button` to `dx add ui/button --write`, while verifying materialized files, docs, receipts, `dx forge verify-package`, local ownership, and no-`node_modules` status.
- `dx forge beta-diagnostics --project <path>` now writes terminal/JSON/Markdown reports for telemetry-free local beta diagnostics, including machine/tool versions, command durations, bounded Cargo cache-use counts, privacy notes, and skipped optional browser/live-framework checks without serializing environment variable values.
- `dx forge launch-copy-review --project <path>` now reviews final public beta launch copy against route comparison, source-owned package review, and static competitor evidence, blocking universal npm/framework replacement claims while preserving approved source-owned security and static/no-runtime performance highlights.
- `auth/better-auth` Forge receipts and generated package docs now explain required Google OAuth env vars, the local callback URL, Forge-owned files, and app-owned production responsibilities.
- `dx check` now validates `auth/better-auth` source-owned packages for missing env examples, unsafe redirect defaults, and stale receipts so OAuth starter packages stay reviewable before launch.
- `dx forge scorecard` now writes a public package scorecard covering `shadcn/ui/button`, `shadcn/ui/card`, `dx/icon/search`, and `auth/better-auth`, including source-owned/no-`node_modules` claims, registry integrity status, and honest launch boundaries.
- `dx forge scorecard --project <path>` now merges the public scorecard with local manifest, package docs, rollback receipt, launch-gate, and `dx check` evidence without creating `node_modules`.
- `dx forge scorecard --output <path>` now preserves score drift history under `.dx/forge/scorecard-history`, including immutable JSON snapshots, a newest-first `index.json`, a reviewable `index.md`, package counts, verified/source-owned counts, and score deltas without creating `node_modules`.
- `dx forge verify-package <id>` now checks one source-owned package at a time for curated registry integrity, local package docs, update preview traffic, rollback evidence, and project scorecard evidence, with terminal/JSON/Markdown output and `--fail-under` scoring while still creating no `node_modules`.
- `dx forge verify-package --all` now verifies every materialized Forge package plus the current launch package set in one aggregate command, reports missing launch packages, supports terminal/JSON/Markdown output and `--fail-under`, and still creates no `node_modules`.
- Forge golden schema fixtures now cover source manifests, receipts, doctor reports, package scorecards, and release proof by generating real temp-project artifacts and checking required JSON paths without creating `node_modules`.
- `dx forge evidence --output <path>` now preserves release-proof history under `.dx/forge/release-proof-history`, including immutable JSON snapshots, newest-first `index.json`, `index.md`, check-score deltas, registry/package summaries, and latest benchmark fixture mode.
- `dx forge smoke` now runs the launch package path outside unit tests by materializing `shadcn/ui/button`, `shadcn/ui/card`, `dx/icon/search`, and `auth/better-auth`, running strict check, doctor, verify-package all, and package scorecard evidence, then writing terminal/JSON/Markdown reports while confirming no `node_modules`.
- Public Forge Markdown snapshot fixtures now lock the scorecard and release-proof launch copy with normalized timestamps and temp paths, so public claims about curated packages, no `node_modules`, and current limitations cannot drift accidentally.
- Forge update simulation fixtures now cover `dx/icon/search` and `auth/better-auth`: selected-icon reviewed yellow edits record explicit review receipts and rollback cleanly, while auth missing/security-sensitive states become red quarantine reports and blocked writes without creating `node_modules`.
- `dx prove vertical --fixture forge-site --out public --write` now generates the compact public `/forge` page from real release-proof and package-scorecard models after materializing the Forge packages, while still emitting crawlable HTML, DXPK assets, Forge receipts, and no `node_modules`.
- Compact `/forge` launch-page snapshots now lock the generated page source and crawlable HTML with timestamps and temp paths normalized, covering release-gate, scorecard, package cards, benchmark evidence, honest boundaries, and no `node_modules`.
- `dx forge launch-page --project . --out public --write` now wraps the compiler-backed Forge-site fixture, writes `/forge` source, crawlable HTML, DXPK, runtime fixture, proof summary, Forge manifest, and receipts, and supports dry-run plus terminal/JSON/Markdown reporting without creating `node_modules`.
- The compact `/forge` route now renders scorecard-backed package detail sections for `shadcn/ui/button`, `shadcn/ui/card`, `dx/icon/search`, and `auth/better-auth`, including provenance, advisory coverage, license-review metadata, public claims, honest boundaries, and local evidence without creating `node_modules`.
- `dx forge launch-page` and the direct Forge-site vertical proof now write `public/forge.claims.json`, a public claims manifest that maps launch-page claims to their source model fields, verification status, and evidence while dry-run still writes nothing.
- `dx forge smoke` now runs the integrated public launch artifact path in one temp project: compact `/forge` page generation, smoke benchmark history, `dx forge evidence` JSON, `dx forge scorecard` JSON, proof summary, claims manifest, and no `node_modules`, with artifact paths recorded in the smoke report.
- `dx forge smoke --ci --fail-under <score>` now emits a single parseable JSON smoke report for automation, quiets nested launch/evidence output, requires no secret environment variables, and still fails below the requested score.
- `dx forge verify-package --all` now keeps local vertical route tracking packages out of curated registry verification, so compiler-owned local source receipts do not fail package integrity checks meant for Forge registry packages.
- `benchmarks/measure-vertical-proof.ts` now supports `DX_VERTICAL_PROOF_MODE=forge-site`, generates the real compiler-backed compact `/forge` launch page, serves extensionless `/forge`, records the route URL, route delivery mode, DXPK proof-artifact status, runtime asset status, and can enforce compact `/forge` delivery budgets with `DX_VERTICAL_BUDGET_GATE=1`.
- Static routes with no compiler-derived interactions now write crawlable HTML plus the canonical `.dxp` proof artifact without injecting the DXPK runtime script or writing a `.dxp.js` runtime asset; interactive counter routes still emit the runtime fixture.
- The compact `/forge` launch page no longer carries the artificial review counter, so it now uses static route delivery while preserving the canonical DXPK packet artifact and claims manifest.
- The compact `/forge` launch page now writes detailed provenance, advisory, license, docs, rollback, and local package proof to `public/forge.evidence.json` instead of carrying that non-critical detail in the first route payload.
- The latest compact `/forge` launch benchmark snapshot records 5,140 decoded bytes, 1,240 Brotli-estimated bytes, 1.866 ms HTTP median, 38.3 ms Chrome load-event median, 1 HTTP resource, 2 Forge packages, static route delivery, no runtime asset, a canonical `.dxp` proof artifact, and a passing delivery budget.
- `benchmarks/compare-forge-launch-delivery.ts` now generates `benchmarks/reports/forge-launch-delivery-comparison.{json,md}`, comparing the current static/no-runtime `/forge` route with the historical DXPK-runtime `/forge` baseline and the interactive combined package vertical.
- The latest delivery comparison records static/no-runtime `/forge` at 5,140 decoded bytes / 1,240 Brotli bytes, historical DXPK-runtime `/forge` at 27,722 decoded bytes / 4,786 Brotli bytes, and the combined interactive package vertical at 10,867 decoded bytes / 3,105 Brotli bytes.
- `dx forge evidence` now keeps a dedicated `latest_forge_route_benchmark` field alongside the generic latest benchmark, so launch evidence surfaces the current `/forge` payload and browser timing even when other verticals are benchmarked later.
- `dx forge scorecard --history <index.json>` now includes `latest_forge_route_benchmark` in JSON output and a "Latest /forge Payload And Browser Timing" section in Markdown/terminal output, while preserving the scorecard history writer around the core package scorecard.
- `dx forge smoke` now runs launch-page quality checks for heading structure, SEO/static-route shape, unsafe links, and claims-manifest consistency, and it writes the smoke benchmark history before page generation so `/forge` claims can verify current payload evidence.
- `docs/forge-ci-smoke.md` now documents a secret-free Forge CI smoke lane, artifact lane, and optional Chrome-backed launch-budget lane without requiring R2 credentials, browser plugins, or a full workspace build.
- `dx forge ci --project <path> --out <dir>` now wraps the secret-free Forge smoke path and writes reviewable CI artifacts, including smoke JSON/Markdown, launch evidence, scorecard, benchmark history, `/forge` HTML, claims, evidence model, proof summary, and DXPK packet.
- `dx forge ci --format markdown` now prints a reviewable CI summary linking every generated artifact, the `/forge` route artifact, claims/evidence models, proof packet, and `/forge/ci` source evidence without requiring R2 secrets or `node_modules`.
- `dx forge ci --verify-artifacts <dir>` now validates an existing Forge CI artifact bundle without re-running smoke, checking required files, JSON parseability, `/forge` route evidence mapping, no-`node_modules` proof, claim statuses, and secret-environment marker leaks.
- `dx forge badge --project <path> --output <path>` now writes a Shields-compatible release-readiness JSON badge derived from Forge smoke, release proof, package scorecard, launch-page quality, no-`node_modules` status, and the latest `/forge` benchmark snapshot; `dx forge ci` now includes the same `forge-readiness-badge.json` artifact.
- `benchmarks/measure-vertical-proof.ts` now reads compact `/forge` launch-budget thresholds from `[forge.launch_budget]` in `dx.config.toml`, supports `DX_VERTICAL_BUDGET_CONFIG` for alternate config files, and preserves the existing `DX_FORGE_SITE_MAX_*` environment overrides as the highest-priority values.
- `dx forge ci` now writes `forge-triage.md`, a reviewable failure-triage artifact covering smoke findings, launch-page quality checks, strict launch-gate findings, package/evidence gates, readiness score, no-`node_modules` status, and the latest `/forge` budget signal; the launch-budget benchmark lane also writes `benchmarks/reports/vertical-proof-triage.md` before failing an enforced budget gate.
- `/forge/ci` failure fixtures now lock failing readiness, launch-page quality, no-`node_modules`, and budget-gate states, including an explicit `ci-budget-gate` claims-manifest entry marked `needs-review` when the latest `/forge` benchmark is not release-ready.
- `scripts/ci/forge-ci.ps1` and `.github/workflows/forge-ci.yml` now prepare a secret-free GitHub Pages bundle for `forge-readiness-badge.json` and `/forge/ci/`, while `docs/forge-ci-smoke.md` documents the manual no-secret publish path and public URLs.
- Forge CI artifact retention is now explicit: `dx-forge-ci` evidence keeps 30 days, `dx-forge-pages` preview keeps 14 days, and `docs/forge-ci-smoke.md` documents local cleanup plus the minimal evidence packet to preserve during release-gate failures.
- `benchmarks/measure-vertical-proof.ts` now measures `/forge/scorecard` and `/forge/ci` alongside `/forge`, and `benchmarks/reports/forge-public-route-comparison.md` records all three public Forge routes as static/no-runtime outputs: `/forge` at 5,140 decoded bytes / 1,240 Brotli bytes, `/forge/scorecard` at 4,189 decoded bytes / 1,040 Brotli bytes, and `/forge/ci` at 3,309 decoded bytes / 863 Brotli bytes.
- The Forge CI fixture now asserts the exact artifact set, confirms the readiness badge reports `no_node_modules`, checks no `CLOUDFLARE_R2_*` or `DX_FORGE_R2_LIVE` dependency leaks into CI review artifacts, and verifies the temp project still has no `node_modules`.
- `dx prove vertical --fixture forge-ci --out public --write` now generates a public `/forge/ci` route from the same Forge smoke and readiness badge evidence models used by `dx forge ci`, writes crawlable static HTML, a canonical DXPK proof artifact, a claims manifest, and no runtime asset or `node_modules`.
- `dx prove vertical --fixture forge-evidence --out public --write` now generates a public `/forge/evidence` index route linking the launch page, scorecard, CI evidence, readiness badge, claims manifests, evidence models, and benchmark comparisons while still writing static HTML, a canonical DXPK proof artifact, a claims manifest, and no runtime asset or `node_modules`.
- `dx forge public-evidence --format terminal|json|markdown --output <path>` now exports the same public evidence map used by `/forge/evidence`, including route, package scorecard context, readiness badge, claims, evidence models, and benchmark comparison links without creating `node_modules`.
- `dx forge public-evidence --verify <dir>` now validates an already exported public evidence directory without regenerating routes, checking `/forge/evidence` HTML links, claims/proof JSON, DXPK header, readiness badge, route comparison coverage, secret markers, and no `node_modules`.
- `benchmarks/measure-vertical-proof.ts` now enforces delivery budgets for `/forge/scorecard` and `/forge/ci` in addition to `/forge`, with route-specific `dx.config.toml` sections and `DX_FORGE_SCORECARD_MAX_*` / `DX_FORGE_CI_MAX_*` environment overrides documented for CI.
- The public evidence fixture now composes the public `/forge/scorecard`, `/forge/ci`, and `/forge/evidence` route artifacts with the readiness badge and benchmark reports, verifies every `/forge/evidence` link points to an existing public artifact, scans the bundle for secret-environment markers, and proves the route fixture path still creates no `node_modules`.
- `dx forge release-notes --project <path> --history <index.json> --format terminal|json|markdown` now generates public Forge release notes from the smoke report, release proof, package scorecard, latest `/forge` route benchmark, and honest launch limitations without requiring secrets or creating `node_modules`.
- `dx forge release-dashboard --project <path>` now verifies CI artifacts, the Pages publish bundle, release notes readiness, public evidence links, and the four-route public comparison in one 0-100 release gate without requiring secrets or creating `node_modules`.
- `docs/forge-public-launch-checklist.md` now gives operators one launch sequence tying `dx forge ci`, Pages verification, release notes, public evidence, release-dashboard gating, benchmark comparison, no-`node_modules` proof, and secret-marker checks into a reviewable public release flow.
- `scripts/ci/forge-ci.ps1` now runs the secret-free `dx forge release-dashboard` gate after Pages bundle verification, copies the checked-in public route comparison into `.dx/ci`, writes `forge-release-dashboard.md`, and verifies the dashboard temp project creates no `node_modules`.
- The full public launch evidence suite was rerun for `/forge`, `/forge/scorecard`, `/forge/ci`, and `/forge/evidence`; `benchmarks/reports/forge-public-route-comparison.*` now records 19,629 decoded bytes, 4,248 Brotli-estimated bytes, four static/no-runtime public routes, and passing route budgets, while `benchmarks/reports/forge-public-release-history.*` records the passing 93/100 release-dashboard gate with no findings.
- `dx forge release-history` now records release-dashboard JSON plus public route comparison JSON into `benchmarks/reports/forge-public-release-history.{json,md}`, de-duplicates repeated evidence, and writes no project files or `node_modules`.
- Public release-history JSON is now covered by the golden schema fixture, and release-history Markdown has a normalized snapshot for dashboard score, route totals, findings, and no-`node_modules` behavior.
- `dx forge release-history` now records release-regression findings for dashboard score drops, public payload growth, missing public routes, failed dashboard gates, and failed route budgets while preserving no-`node_modules` behavior.
- `dx prove vertical --fixture forge-releases --out public --write` now generates a static `/forge/releases` route from `forge-public-release-history.json`, writes crawlable HTML, DXPK proof, claims JSON, source manifest state, and no runtime asset or `node_modules`.
- `scripts/ci/forge-ci.ps1` now records `forge-public-release-history.{json,md}` after the release-dashboard JSON gate, guards the artifact lane against `node_modules`, and `.github/workflows/forge-ci.yml` verifies those release-history artifacts before upload.
- `docs/forge-public-launch-checklist.md` and `docs/forge-ci-smoke.md` now document how to preserve `.dx/ci` release-history evidence, promote `benchmarks/reports/forge-public-release-history.{json,md}`, and publish `/forge/releases/` only from reviewed paired JSON/Markdown artifacts.
- `benchmarks/measure-vertical-proof.ts` now measures `/forge/releases` as a first-class static/no-runtime public route, copying the promoted release-history fixture into the benchmark temp project and enforcing a dedicated `compact-forge-releases` budget.
- `benchmarks/reports/forge-public-route-comparison.*` now records five public Forge routes, including `/forge/releases` at 4,563 decoded bytes, 855 Brotli-estimated bytes, 1.763 ms HTTP median, 11.3 ms Chrome load-event median, static delivery, no runtime asset, and a passing budget.
- `benchmarks/reports/forge-public-release-history.*` now records the latest five-route public comparison with 24,192 decoded bytes and 5,103 Brotli-estimated bytes total; the intentional added-route payload increase is visible as release-history regression evidence for operator review.
- `dx forge release-bundle --project <path> --out <dir>` now assembles `/forge`, `/forge/scorecard`, `/forge/ci`, `/forge/evidence`, `/forge/releases`, clean-route indexes, DXPK packets, badge JSON, CI smoke artifacts, public route comparison, and release history into one verified publish folder.
- `dx forge release-bundle --verify <dir>` now validates an existing public Forge bundle without regenerating routes, checking required artifacts, DXPK headers, route claims where available, clean-route parity, public route comparison coverage, release-history coverage, secret markers, and no `node_modules`.
- Release bundles now include `forge-release-manifest.json` and `forge-release-manifest.md`, recording BLAKE3 hashes, byte counts, artifact type, route ownership, and an unsigned review digest for every public launch artifact; verification fails if a bundled artifact drifts after manifest generation.
- `dx forge release-review --project <path>` now joins the release-dashboard gate, verified release bundle, bundle manifest digest, launch changelog, public route comparison, and release history into one JSON/Markdown/terminal human signoff report without creating `node_modules`.
- The secret-free Pages preview bundle now includes `/forge/releases` beside `/forge/ci`, with route-specific proof files, clean-route indexes, DXPK checks, claims validation, workflow/docs coverage, and backward-compatible `proof.json` for `/forge/ci`.
- The secret-free Pages preview bundle now includes `/forge/changelog` beside `/forge/ci` and `/forge/releases`, with route-specific claims, proof, DXPK, clean-route index, CI script, workflow, docs, and stable Pages shape snapshots.
- Release-bundle manifest and Pages preview shape now have stable JSON and Markdown snapshot fixtures, and the golden schema contract covers the release-bundle manifest fields alongside existing public release proof contracts.
- `dx forge release-history` now reads `[forge.release_history]` from `dx.config.toml` for reviewed route-addition and payload-growth thresholds, while keeping strict zero-growth defaults for unexplained public payload drift, existing-route growth, missing routes, failed budgets, and dashboard failures.
- `dx forge launch-changelog` now turns release-history records into terminal/JSON/Markdown public launch notes, compares the latest record against the previous one, reports added/removed/changed routes and regression findings, and states honest scope limits instead of claiming live traffic or universal npm replacement coverage.
- `dx forge release-bundle` now includes `forge-public-launch-changelog.json` and `.md`, verifies that the changelog is passing and carries honest scope limits, and records both files in the BLAKE3 release manifest as `launch-changelog` artifacts.
- `dx prove vertical --fixture forge-changelog --out public --write` now generates a static `/forge/changelog` route from launch-changelog evidence, writing crawlable HTML, a claims manifest, DXPK proof packet, source manifest state, and no runtime asset or `node_modules`; release bundles now include the route with clean-route index and proof artifacts.
- `dx forge release-dashboard` now checks launch-changelog readiness by building an in-memory release-history preview from the same reviewed dashboard and route-comparison evidence, then requiring the changelog generator to pass with honest-scope guardrails instead of treating it as a marketing-only gate.
- Forge launch-changelog JSON shape, Markdown output, and golden schema contracts now have snapshot coverage, including release-bundle manifest and Pages preview additions through the existing bundle snapshot lane.
- The earlier public route-comparison snapshot and JSON shape contract locked the six-route public surface including `/forge/changelog`, and `/forge/changelog` launch-budget evidence now has a dedicated golden JSON shape fixture.
- Forge release-dashboard, release-review, release-bundle verification, and the PowerShell CI artifact lane first treated the six-route public surface as required, then the active public route-comparison guard expanded to require `/forge/adoption` as the seventh measured static route.
- The launch-bundle temp-project smoke now runs release-history, launch-changelog, `dx forge release-bundle`, `dx forge release-bundle --verify`, release-dashboard, and the six-route comparison guard across isolated temp projects, while scanning public artifacts for secret markers and proving no `node_modules` is created.
- `docs/forge-public-launch-handoff.md` now gives release reviewers a final publication packet for claim review, launch changelog review, release-bundle BLAKE3 manifest hashes, route budgets, Pages artifacts, secret-marker checks, and no-`node_modules` signoff.
- `benchmarks/compare-forge-static-competitors.ts` now writes `forge-static-competitor-evidence.{json,md}`, comparing measured DX-WWW public Forge static routes against generous Astro, Svelte, and Next.js static-floor fixtures while explicitly stating that it is not a full framework benchmark and does not prove broad framework replacement.
- `scripts/ci/forge-ci.ps1` now preserves launch-changelog JSON and Markdown under `.dx/ci` after release-history recording, and the GitHub Actions workflow verifies those files before uploading the normal Forge CI evidence packet.
- Public route benchmarking now includes `/forge/changelog` beside `/forge`, `/forge/scorecard`, `/forge/ci`, `/forge/evidence`, and `/forge/releases`; the six-route comparison records 28,731 decoded bytes, 6,014 Brotli-estimated bytes total, and `/forge/changelog` at 4,539 decoded bytes, 911 Brotli-estimated bytes, 2.993 ms HTTP median, 11.6 ms Chrome load-event median, static delivery, no runtime asset, and a passing budget.
- The focused `dx-serializer` warning-debt slice now documents the touched public parser, schema, machine, LLM, formatter, watcher, and serializer-output APIs; `cargo check --manifest-path ..\serializer\Cargo.toml --lib` emits zero warnings for the serializer library, so CI output is no longer buried under rustdoc noise from this crate.
- `dx forge adoption-smoke` now creates an app-shaped clean project, materializes the launch packages through the real Forge smoke/release-bundle path, publishes `/forge`, `/forge/scorecard`, `/forge/ci`, `/forge/evidence`, `/forge/releases`, and `/forge/changelog` into `public/`, writes adoption and release-bundle evidence under `.dx/forge/adoption-smoke`, and verifies no `node_modules`.
- `benchmarks/compare-forge-medium-route.ts` now writes `benchmarks/reports/forge-medium-route-comparison.{json,md}`, a deterministic medium-route fixture with 12 repeated cards, 6 form fields, 8 route links, and static evidence rows for DX-WWW, Astro, Svelte, HTMX, and Next.js. The latest fixture is honest that it is not a full framework benchmark; generous static floors rank Next.js at 1,436 B Brotli, Astro at 1,438 B, Svelte at 1,441 B, DX-WWW at 1,454 B, and HTMX at 1,511 B.
- `benchmarks/compare-forge-large-content.ts` now writes `benchmarks/reports/forge-large-content-comparison.{json,md}`, a large-content fixture with 8 repeated sections, 96 repeated evidence rows, 3 source-owned package metadata entries, and an explicit first-route payload budget. The latest DX-WWW row stays inside budget at 28,008 decoded bytes, 1,534 B Brotli, and 505 B package metadata, while the report honestly ranks generous static floors ahead of DX-WWW on Brotli for this fixture.
- `dx forge trust-policy --write-policy` now writes `.dx/forge/trust-policy.json`, and `dx forge trust-policy --format terminal|json|markdown --fail-under <score>` reports allowed curated packages, blocked lifecycle/git/persistence/IOC shapes, curated advisory fixtures, declared-only license review state, provenance placeholders, and package-owner responsibilities without creating `node_modules`.
- Curated launch packages now carry explicit `curated-fixture` advisory metadata with provider `dx-forge-curated-advisory-fixture`, stable review timestamps, and no-known-finding notes, so scorecard and trust-policy reports distinguish "review fixture present, no live advisory feed yet" from missing advisory data.
- `dx forge release-trend --write-history` now records `benchmarks/reports/forge-release-readiness-trend.{json,md}`, comparing the latest public release bundle, medium-route fixture, large-content fixture, and trust-policy score against prior trend snapshots. The current trend report passes at 94/100 with public bundle 93, medium route 100, large route 100, and trust-policy 85.
- `docs/forge-real-project-adoption.md` now gives developers a reproducible clean-project Forge adoption path with exact local commands, expected `.dx/forge` and `public/forge*` artifacts, strict check and release-bundle verification steps, release-readiness trend evidence, no-`node_modules` proof, R2-secret boundaries, and honest non-replacement claims.
- The real-project adoption smoke now writes `pages/forge-adoption.html`, an app-shaped route that references `shadcn/ui/button`, `shadcn/ui/card`, `dx/icon/search`, and `auth/better-auth` together, asserts generated package docs for all four packages, and runs `dx check --strict-forge --fail-under 90` against the temp app without creating `node_modules`.
- `dx forge adoption-report --project <path>` now summarizes an existing project's adoption evidence, including project structure, source-owned package IDs, receipts, package docs, strict `dx check` status, public Forge route artifacts, release-bundle verification, no-`node_modules` proof, and honest scope boundaries in terminal, JSON, or Markdown output.
- `benchmarks/compare-forge-live-frameworks.ts` now writes `benchmarks/reports/forge-live-framework-harness.{json,md}`, keeping deterministic static-floor evidence separate from opt-in live Astro, Svelte, HTMX, and Next.js local build rows. Live rows run only when `DX_FORGE_LIVE_FRAMEWORKS=1`, never install packages, skip missing `node_modules` instead of inventing numbers, and report output-byte summaries for already-installed baseline projects.
- `benchmarks/measure-forge-adoption-browser-smoke.ts` now creates a clean local Forge adoption app, serves all six public Forge routes, verifies headings, links, static/no-runtime shape, required DXPK/proof/claims artifacts, no `node_modules`, HTTP route timing, and optional local Chrome load timing. The latest report passes at 100/100 with browser evidence enabled.
- `benchmarks/measure-forge-package-update-rehearsal.ts` now creates a clean local Forge adoption app and proves the package-update behavior across green writes, yellow default blocking, reviewed yellow receipts, red quarantine, rollback restoration, strict-check evidence, and no `node_modules`. The latest report passes at 100/100.
- `dx prove vertical --fixture forge-adoption --out public --write` now generates a static `/forge/adoption` route from the real `DxForgeAdoptionReport`, including source-owned package metrics, exact adoption commands, artifact paths, release-bundle evidence, honest launch boundaries, a claims manifest, DXPK proof packet, and no runtime asset or `node_modules`.
- `dx forge ci` now writes `forge-adoption-smoke.json`, `forge-adoption-report.json`, `forge-adoption-report.md`, `forge-adoption-page.html`, and the static `/forge/adoption` route artifacts into the secret-free CI artifact bundle; `dx forge ci --verify-artifacts` validates the adoption report, no-`node_modules` proof, route claims, route proof, DXPK packet, and clean-route index without R2 secrets or a full workspace build.
- `dx forge ci --verify-pages` and `scripts/ci/forge-ci.ps1` now include `/forge/adoption` in the Pages preview bundle, using a separate temp adoption project so the existing `/forge/ci`, `/forge/releases`, and `/forge/changelog` route fixtures stay stable.
- Public route benchmarking now includes `/forge/adoption` beside `/forge`, `/forge/scorecard`, `/forge/ci`, `/forge/evidence`, `/forge/releases`, and `/forge/changelog`; the seven-route comparison records 37,196 decoded bytes, 7,284 Brotli-estimated bytes total, and `/forge/adoption` at 8,465 decoded bytes, 1,270 Brotli-estimated bytes, 2.815 ms HTTP median, 21.1 ms Chrome load-event median, static delivery, no runtime asset, and a passing budget. The report explicitly treats this as reproducible local adoption evidence, not deployed-user proof or a market-usage claim.
- `dx forge release-bundle --include-adoption` now adds the adoption smoke/report/page artifacts plus `/forge/adoption` HTML, clean-route index, claims, DXPK, and proof files to the release bundle while the default release-bundle command still verifies the stable six-route operator bundle. Bundle verification auto-detects adoption-inclusive bundles and applies the seven-route public comparison/history checks.
- Forge adoption report and `/forge/adoption` route artifacts now have golden JSON schema coverage plus normalized JSON/Markdown snapshots for the adoption report, claims manifest, and proof summary, so public adoption evidence drift is caught by targeted tests.
- `docs/forge-adoption-launch-checklist.md` now provides the adoption-first release flow, tying clean-project adoption smoke, `dx check --strict-forge`, adoption report JSON/Markdown, `/forge/adoption` route proof, `dx forge ci` artifacts, Pages preview verification, benchmark evidence, adoption-inclusive release-bundle verification, and honest non-replacement claims into one reviewable checklist.
- `benchmarks/compare-forge-static-competitors.ts` now includes HTMX static floors and a focused `/forge/adoption` browser-benchmark section, comparing the measured DX-WWW adoption route from `forge-public-route-comparison.json` against deterministic Astro, Svelte, HTMX, and Next.js static-floor fixtures without package installs.
- `benchmarks/measure-forge-source-owned-package-review.ts` now writes `benchmarks/reports/forge-source-owned-package-review.{json,md}`, joining the example app's package docs, receipts, curated advisory placeholders, verify-package output, local-edit yellow review, rollback rehearsal, and no-`node_modules` proof into one review artifact. The latest report passes at 100/100.
- `scripts/ci/forge-ci.ps1` now builds the `dx-www` binary once for the artifact lane, resolves the built executable, and runs Forge artifact/page/release commands directly through that executable instead of repeating `cargo run`, reducing repeated Cargo startup output and warning noise on low-resource CI machines.
- Forge golden schema fixtures now cover Pages bundle verification, release notes JSON, public evidence JSON, and public route comparison JSON so release dashboards depend on stable, reviewable evidence contracts.
- Public Forge Markdown snapshots now lock the `/forge/evidence` evidence-map copy and the four-route public comparison summary with timestamps normalized, making public launch copy drift visible in targeted tests.
- `dx forge ci --verify-pages <dir>` now validates the secret-free GitHub Pages publish bundle before deploy, checking `forge-readiness-badge.json`, `/forge/ci`, `/forge/releases`, `/forge/changelog`, and `/forge/adoption` HTML plus clean-route index parity, route claims, route-specific proof files, `DXPK` packet presence, no secret marker leaks, and no `node_modules`; `scripts/ci/forge-ci.ps1` runs this gate after preparing `.dx/forge-pages`.
- Public route benchmarking now includes `/forge/evidence` beside `/forge`, `/forge/scorecard`, and `/forge/ci`; the latest static/no-runtime comparison records all four routes at 19,626 decoded bytes and 4,244 Brotli-estimated bytes total, with `/forge/evidence` measuring 6,985 decoded bytes, 1,105 Brotli bytes, 1.389 ms HTTP median, and 13.1 ms Chrome load-event median.
- Forge golden schema fixtures now cover CI evidence contracts for `forge-smoke`, `forge-triage`, `forge-readiness-badge`, and `/forge/ci` claims, using real generated smoke/badge/triage artifacts and the same CI claims-manifest builder as the public route.
- `.github/workflows/forge-ci.yml` and `scripts/ci/forge-ci.ps1` now provide reusable CI templates for GitHub Actions and generic PowerShell runners, using targeted `cargo check`, Forge smoke, formatting, diff hygiene, and `dx forge ci` artifact generation without a full workspace build or secret environment.
- `dx forge evidence` now embeds the package scorecard into release proof, including scorecard score, verified/source-owned package counts, `node_modules` count, per-package local evidence, and launch-readiness gating without creating `node_modules`.
- The temp-project launch smoke now materializes `ui/button`, `icon/search`, and `auth/better-auth`, runs `dx check --strict-forge`, `dx forge doctor`, and `dx forge scorecard`, verifies all three packages are tracked, and confirms no `node_modules` is created.
- `dx prove vertical --fixture forge-scorecard --out public --write` now generates a public `/forge/scorecard` route from the real Forge package scorecard model, writes crawlable HTML plus DXPK artifacts, records Forge source state, and keeps the route copy honest that Forge is not a universal npm replacement yet.
- Forge registry packages, source manifests, receipts, generated package docs, public scorecards, and the `/forge/scorecard` route now include structured provenance, advisory, and license-review metadata placeholders with no live vulnerability-coverage claim.
- `dx update <package> --write --accept-yellow --review-note <text>` now preserves reviewed local edits, records an explicit yellow-review policy decision, accepts the local file hash into the Forge receipt, and still refuses red update states.
- `dx update <package> --dry-run` now includes a red-state quarantine report explaining blocked files, remediation, and the fact that Forge did not write, overwrite, or delete user source.
- `dx prove vertical --package icon/search` now has a compiler proof projection for the selected Search icon, so the selected-icon package vertical renders real fallback SVG instead of an unresolved component placeholder.
- `benchmarks/measure-vertical-proof.ts` now supports `DX_VERTICAL_PROOF_MODE=forge-icon` and `forge-combo`, records previous-report comparisons, and the latest combined route measures both `shadcn/ui/button` and `dx/icon/search` together.
- `benchmarks/reports/forge-package-vertical-comparison.*` records the Forge package, selected-icon, and combined-package measurements with package count, tracked file count, bytes, browser timing, DXPK apply status, and interaction status.
- `benchmarks/measure-vertical-proof.ts` now preserves timestamped historical snapshots in `benchmarks/reports/vertical-proof-history/` while still updating the latest `vertical-proof-measurement.*` files.
- `benchmarks/reports/vertical-proof-history/index.json` and `index.md` now summarize historical vertical runs by fixture mode, package count, tracked files, bytes, browser timing, DXPK application, and interaction status.
- `dx forge rollback <receipt.json>` now previews or writes a rollback from a prior Forge receipt, restoring only curated source-owned files whose registry content still matches the receipt hashes.
- Forge rollback tests prove `--dry-run` leaves local edits untouched, `--write` restores the source-owned button file, records a new receipt, and creates no `node_modules`.
- Forge source package metadata now records generator, variant, last accepted update timestamp, and rollback receipt reference, while legacy manifests still parse with safe defaults.
- Forge package variants now support collision-free `--variant <name>` materialization, variant-specific receipts, and targeted updates so local source-owned forks can be named and maintained independently.
- `dx check` now reports Forge stale package counts, packages without accepted updates, oldest accepted update age, rollback-covered packages, missing rollback receipts, and rollback coverage percentage.
- `dx check --strict-forge` now fails launch checks when source-owned packages have stale receipts, missing rollback coverage, or red package traffic while keeping normal score-only checks unchanged.
- Forge registry manifests now verify package integrity, per-file BLAKE3 hashes, byte counts, and local/R2 content blobs before hydrated source packages can be trusted for materialization.
- Forge writes package-facing Markdown docs under `.dx/forge/docs` that explain source ownership, editable files, update commands, policy decisions, and materialized file hashes.
- `dx forge doctor` now runs strict Forge launch-gate checks, curated registry integrity checks, and package-doc coverage checks as one launch-readiness command with terminal, JSON, and Markdown output.
- The real Forge launch smoke runs the `dx-www` binary in a temp project, materializes `ui/button` and `icon search`, runs strict Forge checks and doctor, and verifies `node_modules` is never created.
- `dx check` now reports Forge package-doc metrics and lowers the Forge section score when materialized packages are missing `.dx/forge/docs` coverage.
- `dx forge docs --write` regenerates package-facing Forge docs from the existing source manifest without rewriting materialized source files; `--dry-run` reports the same package docs plan without writes.
- `dx forge evidence` now bundles strict Forge check status, curated registry verification, rollback/doc coverage metrics, and the latest vertical benchmark history into terminal, JSON, Markdown, or a written report file.
- `docs/forge-launch-limitations.md` now states exactly what Forge v1 does, what it does not replace in npm yet, and which claims are honest for public launch.
- `dx prove vertical --fixture forge-site --out public --write` now generates a public `/forge` route through the real compiler proof path, materializes `shadcn/ui/button` plus `dx/icon/search`, writes DXPK output, records Forge receipts, and keeps the page copy honest that Forge is not a universal npm replacement yet.
- The launch-readiness smoke passed in a temp project with `dx add ui/button`, `dx add icon search`, `dx check --strict-forge`, `dx forge doctor`, and no `node_modules`; the latest combined Forge vertical snapshot records 10,867 decoded bytes, 3,105 Brotli-estimated bytes, 2.139 ms HTTP median, 17 ms Chrome load-event median, DXPK DOM apply success, and working counter interaction.
- Touched `dx-serializer` zero-copy helper surfaces now have explicit unsafe boundary documentation and scoped lint allowances, and the targeted serializer safety tests pass with no unsafe-code warnings from those touched helpers.
- Touched `dx-security` public error/stat/scanner fields now carry rustdoc, and `cargo check -p dx-security` no longer emits security-crate missing-doc warnings from those touched APIs.
- Forge receipts now expose a reviewable logical-to-materialized `file_map` alongside package id, upstream, version, integrity hash, license, file hashes, policy decisions, and risk score.
- `dx forge audit` scores package supply-chain risk, including lifecycle hooks, git dependencies, persistence writes, suspicious obfuscated JavaScript, TanStack IOC shapes, missing license, and missing lock/integrity evidence.
- `dx forge registry init/status/publish/pull` has local registry and opt-in Cloudflare R2 plumbing. R2 dry-runs do not require secrets; writes still require env-based credentials.
- `dx check` combines project, Forge, package, security, and maintainability sections.
- DX-Style browser delivery direction is CSS-first by default; binary style remains an analysis/cache/future patch artifact until measured otherwise.
- Phase 2 wire-format ownership is now documented in `docs/wire-format-audit.md`, covering HTIP v1/v2, compiler DXOB, legacy CLI DXOB, delivery lab packets, Binary Dawn, LiveView/stream/server-fragment prototypes, DXB containers, and DX-Machine serializer formats.
- `www/core/src/delivery/contract.rs` now defines the first canonical `DXPK`/`dxp` contracts for page graphs, component graphs, style graphs, fallback HTML, packet headers, packet kinds, sections, and Forge manifest references.
- The canonical `DXPK` packet codec now encodes and decodes a fixed compatibility header, typed section headers, payload length, packet payload hash, and per-section hashes.
- `dx prove vertical` now emits and decodes the canonical browser-delivered `DXPK` route packet alongside fallback HTML while keeping compiler-side DXOB as the build artifact proof.
- `dx prove vertical --write` now emits a route-local browser fixture that preloads the `.dxp`, decodes the canonical packet in the browser, and applies packet facts to the fallback DOM.
- `dx prove vertical --page <path.html> --component <path.tsx> --write` now drives the compiler-backed vertical-slice proof from the real CLI and writes fallback HTML plus a proof summary.
- `dx prove vertical --page <path.html> --package ui/button --write` now materializes the editable Forge shadcn button package and renders `<Button />` through a v0 compiler proof projection without requiring a hand-supplied `.tsx` file.
- Forge source-owned files now have green/yellow/red traffic classification: clean files stay green, local edits become yellow review findings, and missing/security-sensitive edits become red findings that also surface through `dx check`.
- Forge materialization now has focused tests proving local edits are not overwritten on a second add, `dx add ui/button` creates no `node_modules`, and package lifecycle scripts are not executed.
- `dx update ui/button --dry-run` now produces a reviewable Forge change-set preview for tracked source-owned packages, comparing manifest hashes, local file hashes, and the latest curated registry package without writing project files.
- `dx update ui/button --write` now applies only fully green update change sets, blocks yellow/red local states by default, records a Forge update receipt, and keeps receipt filenames collision-safe under rapid add/update flows.
- Forge update receipts now include structured per-file decisions with before/after hashes, tracked-base hashes, file traffic, change kind, and accepted/rejected outcomes for review and rollback consumers.
- Forge update tests now cover a mixed green/yellow package state and prove `dx update --write` does not partially merge safe-looking files while local edits still require review.
- Forge update tests now cover red missing-file and security-IOC states and prove writes are blocked by default while preserving the local project state.
- `dx check` now surfaces Forge package/file metrics for clean, edited, missing, and blocked source-owned files in JSON, Markdown, and terminal output.
- `dx update` dry-run outcomes now include an explicit review plan for yellow/red source-owned package updates while writes remain green-only.
- `benchmarks/measure-vertical-proof.ts` now defaults to a Forge-materialized `shadcn/ui/button` route while retaining `DX_VERTICAL_PROOF_MODE=component` for the older `.tsx` fixture. The latest post-update report measured 10,669 decoded bytes, 3,026 Brotli-estimated bytes, 1.997 ms HTTP median, 27.8 ms Chrome load-event median, DXPK DOM apply success, and working counter interaction.
- The vertical-slice fallback renderer now walks the parsed nested page/component tree, expands local source-owned components, supports static prop substitution, and renders default slot children without structural stitching.
- `dx prove vertical` now plans or writes Forge local-source receipts for the route page and component inputs. On `--write`, it updates `.dx/forge/source-manifest.json` and writes a reviewable receipt without creating `node_modules` or running package scripts.
- `dx prove vertical` can derive a tiny `micro-js` counter runtime from source-owned state expressions and click handlers, inject stable DOM ids, and include the interaction proof in terminal, JSON, and markdown output.
- The compiler DXOB packet now decodes back into page templates, slots, string-table entries, bindings, event counts, and CSS metadata; the vertical proof reports packet bytes and round-trip status.
- `benchmarks/measure-vertical-proof.ts` now builds the Phase 1 vertical source fixture through `dx prove vertical`, serves the generated proof over localhost HTTP, and records real decoded bytes, compression estimates, Chrome navigation timing, transfer size, DXPK DOM-apply status, and counter-click timing in `benchmarks/reports/vertical-proof-measurement.*`.

## Known Placeholders And Risks

- No remote is configured for `G:\WWW`, so root-level push automation is not available yet.
- `www/cli` is a legacy standalone wrapper, while the active CLI implementation lives in `www/dx-www`.
- The route-local DXPK browser fixture is still a proof harness, not the final production runtime.
- Broader serializer design work is still pending, but the library now checks cleanly without missing-doc warnings in the targeted maintenance lane.
- Existing demo server logs are local artifacts, not release proof.
- The compact public `/forge` route now avoids the runtime asset for static delivery, keeps non-critical package/evidence detail outside the first route payload, has a focused comparison report against DXPK-runtime and package vertical baselines, surfaces `/forge` payload/timing in Forge evidence plus scorecard output, has smoke coverage for launch-page quality, and has CI documentation for the smoke and benchmark sequence.
- The current framework-vs-framework numbers include useful packet labs, but medium/big comparisons are not yet a full real-world browser application suite.
- Phase 1 is complete enough to be treated as a real proof loop: source fixture, CLI compile, Forge receipts, fallback HTML, micro interaction, DXOB decode, and browser measurement. Source package materialization and richer runtime hydration are still pending for later phases.

## Next Best Targets

1. Add a root remote when the public/private repository target is decided.
2. Add an installed-binary upgrade smoke for signed release-bundle upgrades without losing local source-owned edits.
3. Add CI/docs wiring for release-operations and publish-plan artifacts so the hosted beta path is reproducible without local tribal knowledge.
