import type { ResourceLocatorState } from "./types";

export const resourceLocatorState: ResourceLocatorState = {
  mode: "list",
  status: "source-only",
  query: "manifest",
  selectedResourceId: "integrations/n8n-nodes-base/dx-node-source-.dx/build-cache/manifest.json",
  nextPageToken: undefined,
  issue: "Dynamic resource loading must be mediated through the n8n editor-session adapter.",
};

