# DX WWW Axum Template

This template intentionally enables hot reload, devtools, and a route handler. In `server_mode=auto`, `dx dev` selects the full Axum runtime because the project uses development endpoints and server-side request handling.
