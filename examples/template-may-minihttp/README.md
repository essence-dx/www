# DX WWW May Minihttp Template

This template is intentionally static: no devtools, no hot reload, no route handlers, and no server source directory. In `server_mode=auto`, `dx dev` selects the tiny may-minihttp-style runtime.
