# Dx WWW Template

A minimal Dx WWW starter with one page: `app/page.tsx`.

Add routes only when the product needs them.

The starter includes the DX logo assets, the first-party `Icon` component,
DX Style files, Forge/no-node_modules policy, and the extensionless `dx`
config without adding extra App Router pages.
